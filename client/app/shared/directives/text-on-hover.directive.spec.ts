import { TextOnHoverDirective } from './text-on-hover.directive';

describe('TextOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new TextOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
